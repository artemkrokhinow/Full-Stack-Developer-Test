{
  "name": "full-stack-monolith",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npx prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss && npm run db:seed --prefix backend && npm start --prefix backend"
  }
}
